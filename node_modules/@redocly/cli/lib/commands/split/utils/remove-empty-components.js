import { isEmptyObject, } from '@redocly/openapi-core';
export function removeEmptyComponents(openapi, componentType) {
    if (openapi.components && isEmptyObject(openapi.components[componentType])) {
        delete openapi.components[componentType];
    }
    if (isEmptyObject(openapi.components)) {
        delete openapi.components;
    }
}
//# sourceMappingURL=remove-empty-components.js.map